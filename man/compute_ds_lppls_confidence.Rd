% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sliding_window_lppls.R
\name{compute_ds_lppls_confidence}
\alias{compute_ds_lppls_confidence}
\title{Compute Daily DS LPPLS Confidence Indicators Using Original Methodology}
\usage{
compute_ds_lppls_confidence(
  data,
  recent_days = 30,
  window_sizes = NULL,
  clusters = 4,
  progress = TRUE,
  benchmark = TRUE,
  save = FALSE,
  folder = "./ds_lppls_results/"
)
}
\arguments{
\item{data}{A data frame with columns Date, Close, and t (decimal time)}

\item{recent_days}{Number of recent trading days to calculate indicators for (default: 30)}

\item{window_sizes}{Vector of window sizes to use (default: creates ~980 windows like original)}

\item{clusters}{Number of parallel clusters to use (default: 4)}

\item{progress}{Logical, whether to show progress tracking (default: TRUE)}

\item{benchmark}{Logical, whether to show detailed timing information (default: TRUE)}

\item{save}{Logical, whether to save intermediate results (default: FALSE)}

\item{folder}{Output folder path (default: "./ds_lppls_results/")}
}
\value{
Data frame with DS LPPLS Confidence indicators for each recent day
}
\description{
This function implements the exact DS LPPLS Confidence methodology from the thesis,
calculating confidence indicators for recent trading days using multiple window sizes
and proper filtering criteria for each time scale.
}
\examples{
\dontrun{
# Calculate DS LPPLS Confidence for last 30 days (original methodology)
daily_confidence <- compute_ds_lppls_confidence(data, recent_days = 30)

# Calculate for last week only
daily_confidence <- compute_ds_lppls_confidence(data, recent_days = 5)
}
}
